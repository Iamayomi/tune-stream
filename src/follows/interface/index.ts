export interface FollowArtist {
  artistId: number;
  totalFollowers: number;
  isFollowing: boolean;
}

export interface FollowAlbum {
  albumId: number;
  totalFollowers: number;
  isFollowing: boolean;
}
